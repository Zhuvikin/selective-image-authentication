plugins {
    id 'idea'
}

subprojects {
    repositories {
        jcenter()
    }

    apply plugin: 'java'

    dependencies {
        compile 'com.fasterxml.jackson.core:jackson-databind:2.9.0'
    }

    compileJava {
        options.encoding = "UTF-8"
    }
}
